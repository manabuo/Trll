<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:behaviors="clr-namespace:Trll.Mobile.Behaviors;assembly=Trll.Mobile"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Trll.Mobile.Views.HomePage" >
  <StackLayout Orientation="Vertical">
    <StackLayout Orientation="Horizontal">
      <Label Text="{Binding Name}"></Label>
      <Button Text="Search"></Button>
      <Button Text="Notifications"></Button>
      <Button Text="Menu"></Button>
    </StackLayout>
    <ListView IsGroupingEnabled="True"
              GroupDisplayBinding="{Binding TeamName}"
              GroupShortNameBinding="{Binding TeamName}"
              ItemsSource="{Binding Teams}">
      <ListView.Behaviors>
        <behaviors:ListViewSelectedItemBehavior Command="{Binding BoardSelected}"/>
      </ListView.Behaviors>
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal">
              <BoxView Color="Blue"></BoxView>
              <Label Text="{Binding BoardName}"></Label>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>